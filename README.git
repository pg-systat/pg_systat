(This file does not appear in release tarballs.)

If you are using a git checkout, here are the packaging instructions.

Creating Source Packages
------------------------

git checkout-index --prefix=/tmp/pgstat/ -a
cd /tmp/pgstat
cmake CMakeLists.txt
make package_source
